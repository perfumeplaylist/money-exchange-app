import { LoginWidgets } from "@/widgets";

const LoginPage = () => {
  return (
    <main className="min-h-screen m-auto flex flex-col justify-center gap-12 p-4">
      <LoginWidgets />
    </main>
  );
};
export default LoginPage;
